# v1.0.7 社区功能增强 + 代码拆分收尾

> 本文档列出 v1.0.6 之后尚未完成的所有任务，全部完成后删除。

---

## 一、社区功能增强（新增）

### 1.1 评论系统
| 任务 | 说明 |
|------|------|
| 后端：评论数据存储 | 新增 `server/lib/commentStore.ts`，`scripts/community_comments.json`，字段：id、postId、content、author、createdAt |
| 后端：评论 API | `GET /api/community/posts/:id/comments`、`POST /api/community/posts/:id/comments`、`DELETE /api/community/posts/:id/comments/:commentId` |
| 前端：评论展示 | CommunityPostCard 内展示评论列表，超过 N 条自动折叠，点击展开 |
| 前端：评论输入 | 不允许匿名发表评论，必须填写昵称 |

### 1.2 点赞动画
| 任务 | 说明 |
|------|------|
| 右下角 👍 图标 | 固定悬浮在页面右下角（社区页面内） |
| 点击动画 | 点击后从点击位置生成绿色星星，向上飘动并渐隐消失（CSS animation） |

### 1.3 删除帖子/图片
| 任务 | 说明 |
|------|------|
| 后端：删除 API | `DELETE /api/community/posts/:id` |
| 前端：删除按钮 | 帖子卡片增加删除入口，需确认 |
| 删除记录 | 删除操作记录到社区动态侧栏（ActivityBar） |

### 1.4 发帖体验优化
| 任务 | 说明 |
|------|------|
| 允许无图片发帖 | 纯文字帖子，不强制要求上传图片 |
| 按钮改名 | "发布帖子" → "发布帖子/图片" |
| Ctrl+V 粘贴识别 | 打开发布面板后，支持 Ctrl+V 读取剪切板图片并预览 |
| 退出识别 | 关闭发布面板后，不再监听 Ctrl+V |
| 提示文案 | 读取剪切板按钮右侧新增灰色小字："Ctrl+v可识别图片" |

### 1.5 标签
| 任务 | 说明 |
|------|------|
| 新增标签 | #赌桥 |

---

## 二、App.tsx 深度拆分（当前 ~1856 行 → 目标 ~350 行）

### 2.1 提取 Hooks

| 文件 | 内容 | 预计行数 |
|------|------|----------|
| `src/hooks/useDailyPassword.ts` | syncDailyPwd、handleRefreshDailyPwd、applyDailyPwd、shouldRefreshDailyPwd、handleCopyDailyPwd 及 ref/timer/date-watcher useEffect | ~240 |
| `src/hooks/useTheme.ts` | isDarkMode / customTheme / uiPreferences 状态与 localStorage 持久化 | ~80 |

### 2.2 提取页面/面板组件

| 文件 | 内容 | 预计行数 |
|------|------|----------|
| `src/components/SettingsPage.tsx` | 设置页全部 JSX | ~140 |
| `src/components/DailyPwdCard.tsx` | 首页"今日密码"显示卡片 | ~50 |
| `src/components/EditCustomizePanel.tsx` | 编辑模式界面自定义面板（面板改小 + 滑块连续精细调节） | ~80 |
| `src/components/ModeSelectModal.tsx` | 采集模式选择弹窗 | ~30 |
| `src/components/AutoCollectConfigModal.tsx` | 自动采集配置弹窗 | ~100 |

---

## 三、server.ts 拆分（当前 ~1238 行 → 目标 ~120 行）

### 3.1 删除重复类型定义
- `server.ts` L41-213 ~170 行类型定义与 `src/types.ts` 重复，统一复用

### 3.2 提取 lib

| 文件 | 内容 |
|------|------|
| `server/lib/logs.ts` | getBeijingTimeString、readLogs、addLog |
| `server/lib/shape.ts` | ensureVariantShape、ensureGroupShape、normalizeGunName、trimUniqueStrings |
| `server/lib/collectSettings.ts` | 采集配置读写、buildProviderMeta、buildCollectMeta |
| `server/lib/merge.ts` | sortVariantsNewestFirst、mergeGroupVariants、mergeCollectedGroups |
| `server/lib/collector.ts` | runCollector、fetchModelsFromProvider、runSearchCollectorStream |

### 3.3 提取 routes

| 文件 | 内容 |
|------|------|
| `server/routes/builds.ts` | `/api/builds` GET/POST |
| `server/routes/collect.ts` | `/api/collect/*` |
| `server/routes/config.ts` | cookie、settings-file |
| `server/routes/dailyPassword.ts` | `/api/daily-password/*` |

---

## 四、进度记录

### 社区功能增强
| 任务 | 状态 |
|------|------|
| 评论后端存储 + API | ⏳ 待开始 |
| 评论前端展示（折叠） | ⏳ 待开始 |
| 评论输入（禁止匿名） | ⏳ 待开始 |
| 👍 右下角图标 | ⏳ 待开始 |
| 点击飘星动画 | ⏳ 待开始 |
| 删除帖子/图片 API | ⏳ 待开始 |
| 删除按钮与确认 | ⏳ 待开始 |
| 删除记录到动态 | ⏳ 待开始 |
| 允许无图片发帖 | ⏳ 待开始 |
| 按钮改名"发布帖子/图片" | ⏳ 待开始 |
| Ctrl+V 粘贴识别图片 | ⏳ 待开始 |
| 关闭面板后停止 Ctrl+V | ⏳ 待开始 |
| "Ctrl+v可识别图片"提示 | ⏳ 待开始 |
| 新增 #赌桥 标签 | ⏳ 待开始 |

### App.tsx 拆分
| 任务 | 状态 |
|------|------|
| useDailyPassword.ts 提取 | ⏳ 待开始 |
| useTheme.ts 提取 | ⏳ 待开始 |
| SettingsPage.tsx 提取 | ⏳ 待开始 |
| DailyPwdCard.tsx 提取 | ⏳ 待开始 |
| EditCustomizePanel.tsx 提取 | ⏳ 待开始 |
| ModeSelectModal.tsx 提取 | ⏳ 待开始 |
| AutoCollectConfigModal.tsx 提取 | ⏳ 待开始 |
| App.tsx 验证 | ⏳ 待开始 |

### server.ts 拆分
| 任务 | 状态 |
|------|------|
| server.ts 类型去重 | ⏳ 待开始 |
| server/lib/logs.ts | ⏳ 待开始 |
| server/lib/shape.ts | ⏳ 待开始 |
| server/lib/collectSettings.ts | ⏳ 待开始 |
| server/lib/merge.ts | ⏳ 待开始 |
| server/lib/collector.ts | ⏳ 待开始 |
| server/routes/builds.ts | ⏳ 待开始 |
| server/routes/collect.ts | ⏳ 待开始 |
| server/routes/config.ts | ⏳ 待开始 |
| server/routes/dailyPassword.ts | ⏳ 待开始 |
| server.ts 验证 | ⏳ 待开始 |

### 收尾
| 任务 | 状态 |
|------|------|
| 全部验证 (lint + build + 浏览器) | ⏳ 待开始 |
| 更新版本号与 release-notes | ⏳ 待开始 |
| 删除本文档 | ⏳ 待开始 |
