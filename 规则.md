项目规则与目标

[最终目标]
完成所有功能后，将本项目打包上传至1Panel部署，作为一个正常的网站对外提供服务。

[工作流规范 - 双机开发]
随时记录：目前项目在公司和家里两台电脑进行开发。不管是什么改动，哪怕是极微小的代码调整或环境配置，一切都必须记录在进度文档里面。
时间格式：进度记录的时间必须使用精确的“年月日 时:分”格式（禁止使用“刚刚”等模糊描述），确保多端同步时时间线绝对清晰。
切换设备：换电脑开发时，优先查看进度的最新说明以恢复上下文。

[运行机制与核心规则]
基础运行：本地运行在127.0.0.1:3000，前端加Express共同启动，目前数据持久化到src/data.json。
“手动采集”流程：选枪械 -> 选博主 -> 搜索命中视频 -> 多选视频 -> 选模型 -> 测试模型 -> 确认加入网站。
采集链路的绝对约束：
1. 模型固定：默认模型锁定为openai/gpt-oss-120b。
2. 体验顺畅：搜索阶段必须有实时日志状态呈现，禁止只有loading；采集时不触发页面刷新或弹窗闪退。
3. 数据安全：同枪数据合并，单枪最多保留5条，锁定状态(locked)的配置不能被新采集覆盖。
4. 拒绝乱码：全链路彻底杜绝中文乱码。
5. 操作提速：已选中的视频在“确认加入网站”时，直接提取数据即可，绝不允许重新触发搜索。
6. 每次命令任何改动都需要记录到进度.md。