version: '3.8'

services:
  app:
    build: .
    container_name: xiujiao-ai
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
      - ./scripts:/app/scripts
    environment:
      - NODE_ENV=production
      - PORT=3000