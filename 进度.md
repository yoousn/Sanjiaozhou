# 项目进度存档

## [当前节点] 梳理规则与修复环境依赖
**时间：** 2026年04月24日 03:15
**当前状态：**
1. 已经精简并更新了 `规则.md`，明确了后续上传 1Panel 的最终目标。
2. 明确了公司和家里两台电脑开发的交接规则。
3. 发现本地环境执行 Python 采集时报错 `No module named yt_dlp`。
4. **下一步待办**：在本地 Python 环境安装相关依赖以使得搜索及采集功能可以正常跑通。

## [当前节点] 强化工作流规范
**时间：** 2026年04月24日 03:25
**当前状态：**
1. 修改了 `规则.md`，将“一切微小改动都必须记录在进度中”确立为绝对铁律。
2. 等待本地执行依赖安装（`yt_dlp` 等），以验证采集脚本是否可以正常调用。

## [当前节点] 规范时间记录格式
**时间：** 2026年04月24日 03:32
**当前状态：**
1. 修改了 `进度.md` 中的历史时间记录，将“刚刚”替换为具体的“年月日 时:分”格式。
2. 更新了 `规则.md`，增加时间格式的书写规范，确保多端同步时时间线绝对清晰。

## [当前节点] 修复 B 站抓取 SSL EOF 报错
**时间：** 2026年04月24日 03:36
**当前状态：**
1. 发现搜索请求视频详情时，B 站报错 `[SSL: UNEXPECTED_EOF_WHILE_READING]`，原因为 B 站风控主动断开连接。
2. 修改了 `scripts/collect_bilibili_test.py` 中的 `yt-dlp` 调用参数，增加了自动重试、忽略 SSL 校验以及浏览器 UA/Referer 伪装。
3. 等待重新测试搜索命中功能。

## [当前节点] 迁移系统设置页与新增 Cookie 上传功能
**时间：** 2026年04月24日 21:07
**当前状态：**
1. 将原本弹窗式的“外观设置”完全重构成独立的“系统设置”页面（通过左侧导航栏下方齿轮直达）。
2. 新增 Bilibili Cookie（`cookies.txt`）一键上传功能。
3. 在 `server.ts` 和 `collect_bilibili_test.py` 新增 `check-cookie` 功能。用户上传后，后端自动调用 `yt-dlp` 测试抓取博主视频列表，以精准验证 Cookie 是否有效以及是否被风控拦截。

## [当前节点] 完善系统设置页与 Cookie 状态显示
**时间：** 2026年04月24日 21:46
**当前状态：**
1. 修复了页面上残留的“外观设置”字眼，彻底统一为主体“系统设置”页面下的分类。
2. 后端 `server.ts` 新增 `/api/config/cookie/status` 接口，用于查询 `cookies.txt` 的存在状态与最新修改时间。
3. 在系统设置的 Bilibili Cookie 卡片中，新增了 Cookie 当前生效状态与更新时间的实时显示。
4. 清理了 `App.tsx` 中上一步重构留下的冗余代码（重复的 Sidebar 组件调用）。

## [当前节点] 解决多端数据同步问题
**时间：** 2026年04月25日 03:01
**当前状态：**
1. 明确了线上（24h运行）版本与本地开发版本的数据同步流程：部署本地代码前，必须先将线上的 `data.json` 下载并覆盖本地文件。
2. 为此，在“系统设置”页面新增“数据管理”模块。
3. 新增“下载 data.json”按钮，点击后可直接从正在运行的服务下载最新的数据文件，解决了数据覆盖问题。

## [当前节点] 增加配置文件下载与统一日志时区
**时间：** 2026年04月25日 03:28
**当前状态：**
1. 在“系统设置”的“数据管理”模块中，新增了“下载 collect_settings.json”功能，方便多端同步采集和模型配置。
2. 将 `server.ts` 中自动采集日志 (`auto_logs.json`) 的时间记录方式，统一为北京时间（UTC+8）格式。
3. 明确了所有时间相关的日志，最终都应以北京时间为准进行记录或展示。

## [当前节点] 优化 1Panel 部署指令（平滑更新）
**时间：** 2026年04月25日 03:41
**当前状态：**
1. 分析了部署造成停机的原因：先执行 `docker-compose down` 会导致网站在解压和构建期间完全不可用。
2. 更改了 `规则.md` 中的部署命令，采用先 `build` 后台构建新镜像，再 `up -d` 瞬间重启替换的方式，实现了近乎不停机的平滑更新。

## [当前节点] 修复打包及版本冲突残留
**时间：** 2026年04月25日 04:20
**当前状态：**
1. 发现由于某些代码同步/合并行为，项目中残留了 `<<<<<<< HEAD` 冲突标记及重复代码，直接导致了 Docker 部署时的 `esbuild` 编译失败。
2. 修复了 `CollectModal.tsx` 的标签未闭合和变量重复定义。
3. 移除了 `server.ts`、`auto_logs.json` 中的合并标记。
4. 修正了 Python `collect_bilibili_test.py` 中的空白符缩进错误。

## [当前节点] 成功完成平滑更新与进入下一阶段
**时间：** 2026年04月25日 04:46
**当前状态：**
1. 解决了所有语法和标签闭合错误，成功在 1Panel 服务器环境完成 `docker-compose build`。
2. 准备执行 `docker-compose up -d` 瞬间替换容器上线。
3. **下一步待办：** 根据最新的 `jx.md` 断点文档，开始集中攻坚当前手动采集的 3 个核心痛点：搜索无真实进度日志、偶尔的中文乱码、以及“确认加入网站”时因为重复 search 导致的过慢问题。

## [当前节点] 排查部署后无法访问问题
**时间：** 2026年04月25日 04:55
**当前状态：**
1. 执行 `docker-compose up -d` 成功启动容器，但浏览器访问 `192.168.1.3` 提示“拒绝连接”。
2. 提供了三步排查法：检查访问 URL 是否遗漏 `:3000` 端口、检查容器运行日志排查闪退、检查 1Panel 防火墙端口放行状态。
3. 等待排查结果反馈。

## [当前节点] 解决后端容器运行报错（遗留冲突清理）
**时间：** 2026年04月25日 05:10
**当前状态：**
1. 查看 Node 运行日志发现 `server.ts` 中还遗留了一处合并冲突标记（`<<<<<<< HEAD`），导致服务端代码启动闪退。
2. 删除了 `getDefaultCollectSettings` 函数内的冲突字符，彻底排除了语法错误。
3. 需重新构建镜像以应用修复，等待确认网站恢复访问。

## [当前节点] 解释部署指令差异与平滑更新原理
**时间：** 2026年04月25日 05:15
**当前状态：**
1. 详细解释了 `docker-compose up -d --build` 与 `docker-compose build` 后 `docker-compose up -d` 的区别。
2. 阐明了为何不再使用 `docker-compose down`：避免在构建新镜像的漫长过程中造成网站长时间停机。
3. 确认了当前部署指令旨在实现“平滑更新”，将耗时构建与快速服务切换分离，最大化网站可用性。

## [当前节点] 增加每日密码自动抓取功能与轻量化改造
**时间：** 2026年04月25日 15:26
**当前状态：**
1. 探讨了在 Aml.S905l3a (2G内存) 低功耗设备上的极简架构方向，明确了弃用繁重数据库和组件，转向纯 Node.js 内存处理的方针。
2. 编写并修改了 Python 爬虫脚本 (`爬取每日密码.py`)，支持无头模式静默运行及纯净 JSON 输出。
3. 修改了 `server.ts`，新增 `/api/daily-password` (获取缓存) 和 `/api/daily-password/refresh` (触发抓取) 接口。
4. 考虑到 Playwright 占用内存过高，当前正在研究改用 `requests` 或直接抓包 API 的轻量级方案，以进一步降低电视盒子的性能开销。

## [当前节点] 成功解析每日密码API接口并完成轻量化
**时间：** 2026年04月25日 15:35
**当前状态：**
1. 通过网络抓包成功找到每日密码的真实数据 API (`https://www.kkrb.net/getOVData`)。
2. 修复了 `爬取每日密码.py` 脚本，补全 `https://` 协议，并根据返回的 JSON 结构完成了 5 张地图名称与缩写的映射解析。
3. 彻底跑通轻量级 `requests` 方案，不再依赖 Playwright，完美适配 2G 内存的电视盒子。

## [当前节点] 排查 API 接口 404 报错与数据解析
**时间：** 2026年04月25日 15:37
**当前状态：**
1. 测试 Python `requests` 请求目标 API 时遇到 404 Client Error 报错。
2. 分析抓包数据发现，返回的结构 (`YYYYMMDDHHMM: value`) 是每 20 分钟记录一次的时间序列数据，判断为游戏内物价/交易行走势，而非纯粹的每日密码。
3. 怀疑 404 是由于请求方法错误 (GET vs POST) 或 URL 缺失 `/api/` 路由导致，当前正在通过 `Copy as cURL` 方法进一步提取真实的请求头与 Method。

## [当前节点] 成功利用 POST 方法获取每日密码数据
**时间：** 2026年04月25日 15:39
**当前状态：**
1. 通过 cURL 转换确认了 `/getOVData` 接口需要使用 `POST` 方法，并且带有 `application/x-www-form-urlencoded` 表单数据。
2. 成功将 POST 请求参数（headers, data）整合进 `爬取每日密码.py`，完美解决了此前的 404 报错问题。
3. 实现了无需启动浏览器的极速、轻量化抓取每日密码功能。

## [当前节点] 排查 API 请求成功但无数据的问题
**时间：** 2026年04月25日 15:42
**当前状态：**
1. 执行轻量化脚本时，接口 HTTP 状态码 200 正常，但解析结果均为“未发现数据”。
2. 推测原因：移除了 Cookie，或者表单中的 `version` (类似 MD5 Hash) 是动态校验参数，过期导致服务器未返回有效数据。
3. 在 `爬取每日密码.py` 中加入了原始响应拦截机制，等待查看服务器真实的报错信息以决定下一步策略（动态抓取 version 或补回基础 Cookie）。

## [当前节点] 破解动态参数与会话防爬机制
**时间：** 2026年04月25日 15:45
**当前状态：**
1. 根据拦截器输出 `{"code": -101, "msg": "系统繁忙..."}`，确认触发了服务器的反爬验证机制。
2. 引入 `requests.Session()` 自动维持连接状态，在 POST 请求前先发送一次 GET 请求主页以获取新鲜的 `PHPSESSID` 等 Cookie。
3. 使用正则动态抓取网页源码中的 32 位 Hash `version` 参数，彻底攻破其每日变动的校验机制，实现脚本稳定自动化。

## [当前节点] 修复 Version 提取错误与补充防爬 Headers
**时间：** 2026年04月25日 15:47
**当前状态：**
1. 分析日志发现，返回 `-101 系统繁忙` 是因为先前的正则抓取到了错误的 32 位哈希（如 CSS 文件名），导致服务器校验 `version` 失败。
2. 修正了正则表达式，增加 `version` 关键字的严格匹配限制，并在提取失败时安全回退到已知的可用 `version`。
3. 补充了完整的前端 `sec-ch-ua` 等请求头，并动态生成随机 `PHPSESSID`，进一步降低被反爬拦截的概率。

## [当前节点] 破解 checkUAStatus 接口守护机制
**时间：** 2026年04月25日 15:50
**当前状态：**
1. 分析了完整的网络请求抓包日志 (`每日密码详情.txt`)，发现了导致 `-101 系统繁忙` 的根本原因。
2. 该网站设有一道名为 `checkUAStatus` 的安全网关拦截器。必须先携带真实的 `PHPSESSID` 访问该接口并验证 `version` 后，当前 Session 才会被放行。此前强行伪造的 `PHPSESSID` 导致会话直接失效。
3. 移除了代码中伪造的 `PHPSESSID` 逻辑，改由 `requests.Session()` 自然接管。并在获取数据前，增加了一步关键的 `checkUAStatus` POST 请求，彻底跑通了从获取凭证、激活会话到拉取数据的完整闭环。

## [当前节点] 彻底抛弃 Playwright 采用纯 Requests 请求流
**时间：** 2026年04月25日 16:11
**当前状态：**
1. 测试发现，由于快快网络部署了较强的 WAF，即使使用 Playwright 无头模式依然会被浏览器指纹识别拦截，且 Playwright 对 2G 内存的电视盒子负担过重。
2. 深度分析 `每日密码详情.txt`，提炼出完美的 3 步 AJAX 握手流程：`getMenu` -> `checkUAStatus` -> `getOVData`。
3. 重新编写 `爬取每日密码.py`，使用纯 `requests.Session()` 精准模拟完整的网络握手与停顿，实现毫秒级、0 内存负担的终极静默爬取。

## [当前节点] 移除导致 Version 取值错误的贪婪正则
**时间：** 2026年04月25日 16:15
**当前状态：**
1. 在纯 Requests 流程测试中再次收到 `-101 系统繁忙`，且日志显示使用的 version 为 `a356e23...`。
2. 发现是由于代码中残留的兜底正则 `match2` 过于贪婪，错误提取了网页源码中的 CSS/JS 文件哈希值，覆盖了正确的默认 version。
3. 删除了危险的兜底正则，确保脚本严格使用 `945a4c...` 或是精准匹配的 version 参数，再次进行全流程验证。

## [当前节点] 结合 curl_cffi 与三步握手彻底攻破 WAF
**时间：** 2026年04月25日 16:17
**当前状态：**
1. 发现此前使用 `curl_cffi` 模拟底层指纹时，遗漏了至关重要的 `checkUAStatus` 激活步骤，导致依旧被 WAF 拦截返回 `-101`。
2. 删除了脚本中干扰 Node.js 解析的 `print` 打印语句，保证纯净的 JSON 输出。
3. 将 `curl_cffi` (解决 TLS 指纹拦截) 与 3 步 AJAX 握手 (解决应用层 Session 拦截) 完美结合，实现最终稳定抓取，并顺利接入网站后端。

## [当前节点] 敲定最终爬虫方案：极低内存版无头浏览器
**时间：** 2026年04月25日 16:21
**当前状态：**
1. 经多轮对抗，发现目标 WAF 存在严苛的 JS 环境挑战，纯请求级伪装难以保持长期稳定。
2. 最终采用 Playwright 无头模式，但引入了极致的内存优化参数（禁用 GPU、沙盒、扩展）并拦截了所有图片/CSS/字体等无关资源的加载，将内存开销压缩至 150MB 左右的短暂脉冲，成功适配 2G 内存的电视盒子。
3. 移除了 Python 脚本中的友好提示 `print` 语句，确保标准输出为纯净的 JSON 字符串，完美对接 `server.ts` 的 `JSON.parse`。

## [当前节点] 部署准备与完善每日密码自动化展示
**时间：** 2026年04月25日 16:23
**当前状态：**
1. 确认代码已推送至 Gitee 私有仓库，准备后续在 1Panel 配置 Webhook 实现无缝自动化部署。
2. 在 `server.ts` 中补全了每日密码的定时抓取逻辑：每 10 分钟检测一次，以北京时间 00:00 为界，一旦抓取到最新有效密码即停止轮询，直到次日自动重启任务。
3. 修改了 `App.tsx`，在网站上方居中位置新增了“白+绿”专属配色的每日密码展示面板，并同步显示了北京时间日期。

## [当前节点] 优化每日密码复制反馈动画
**时间：** 2026年04月25日 18:18
**当前状态：**
1. 阅读并确认了全局规则文件 `C:\Users\Administrator\.claude\CLAUDE.md`，本次继续遵守“任何命令和改动都记录到 `进度.md`，并以北京时间记录”的要求。
2. 修改了 `src/App.tsx` 中今日密码小方框的复制反馈：点击后会出现深绿色填充动画，并在约 220ms 后自动淡出，同时鼠标移出或失焦时也会立即清除高亮状态。
3. 为复制反馈补充了定时器清理逻辑，避免重复点击或组件卸载时残留状态。

## [当前节点] 排查每日密码面板再次消失
**时间：** 2026年04月25日 18:22
**当前状态：**
1. 用户反馈网页上再次看不到“每日密码”面板，已开始回查前端显示逻辑。
2. 检索 `src/App.tsx` 与 `server.ts` 后确认：后端每日密码接口仍在，但前端当前文件里只剩下复制动画的清理 `useEffect`，原本负责首次加载 `/api/daily-password` 并在必要时触发 `/refresh` 的初始化逻辑已不在页面中。
3. 已定位这就是面板消失的直接原因：`dailyPwd` 状态没有被加载赋值，导致首页条件渲染不成立。

## [当前节点] 统一每日密码点击动画时长
**时间：** 2026年04月25日 18:29
**当前状态：**
1. 用户确认每日密码小方框的深绿色反馈应只受点击后的动画时长控制，不应再受鼠标移开影响。
2. 已移除 `src/App.tsx` 中对应按钮的 `onMouseLeave` 与 `onBlur` 立即清除逻辑，现统一按当前设置的持续时间与过渡速度完整播放。
3. 当前该反馈的可调位置仍为 `handleCopyDailyPwd` 中的定时器时长，以及按钮/覆盖层上的 `duration-500` 过渡参数。

## [当前节点] 接入 GitHub Actions + SSH 自动部署
**时间：** 2026年04月26日 03:13
**当前状态：**
1. 已确定自动部署方案为 GitHub Actions + SSH 到服务器，由服务器在 `/opt/xiujiao-era` 直接 `git pull` 私有仓库代码。
2. 新增了 `.github/workflows/deploy.yml` 与 `scripts/deploy_remote.sh`，工作流会先在 GitHub Runner 执行 `npm ci` 和 `npm run build`，再远程执行 `docker-compose build`、`docker-compose up -d` 和默认自动镜像清理。
3. 修改了 `docker-compose.yml`，将原本整目录挂载 `src` / `scripts` 改成挂载 `runtime/` 下的单个运行态文件，避免自动部署覆盖线上真实数据与 `cookies.txt`。
4. 修改了 `.gitignore` 与 `README.md`，补充运行态文件忽略规则、GitHub Secrets 配置、首次迁移 `runtime/` 目录步骤以及“严禁先 down 再更新”的平滑更新说明。
5. 本次按你的选择保留现有硬编码 API 写法，暂不处理环境变量迁移。
6. 根据审查结果，已将 `src/data.json`、`src/daily_pwd.json`、`scripts/cookies.txt`、`scripts/collect_settings.json`、日志/缓存 JSON 与 Python 缓存文件从 Git 跟踪中移除，后续只保留服务器 `runtime/` 中的真实运行态数据。
7. 根据用户反馈，README 已补充“代码目录与 runtime 目录各自职责”、换服务器时必须迁移的具体文件列表，以及最小迁移方式，方便后续搬迁时直接照做。

## [当前节点] 修复每日密码部署缺少 Playwright 依赖
**时间：** 2026年04月26日 12:36
**当前状态：**
1. 排查确认 `爬取每日密码.py` 当前实际仍然直接依赖 Playwright 启动 Chromium，无头抓取并监听 `getOVData` 响应，因此线上报错 `No module named 'playwright'` 的根因是镜像内未安装对应 Python 依赖。
2. 修改了 `Dockerfile`，将基础镜像从 `node:20-alpine` 切换为更适合 Playwright Chromium 的 `node:20-bookworm-slim`，并补装 `python3`、`python3-pip`、`python-is-python3`。
3. 在镜像构建阶段新增 `pip3 install playwright` 与 `python -m playwright install --with-deps chromium`，确保容器内既有 Python 包，也有可用的 Chromium 浏览器运行时。
4. 下一步待办：执行部署，让服务器重新构建镜像并验证每日密码抓取恢复。

## [当前节点] 系统设置页增加每日密码手动获取按钮
**时间：** 2026年04月26日 12:47
**当前状态：**
1. 修改了 `src/App.tsx`，在系统设置页“每日密码日志”卡片右上角新增一个小号“获取”按钮，点击后可立即手动抓取最新每日密码，不必再等待后端 10 分钟轮询。
2. 新增前端加载态 `isRefreshingDailyPwd` 与手动刷新处理函数，调用现有 `/api/daily-password/refresh` 接口，并在请求过程中显示转圈与禁用按钮，防止重复点击。
3. 刷新成功后会立即更新首页顶部“今日密码”展示；无论成功或失败，都会立刻重新拉取“每日密码日志”，保证最新日志即时可见。
4. 下一步待办：启动前端并实际点按按钮，确认加载态、Toast 提示与日志即时刷新全部正常。

## [当前节点] 修复部署阶段 Playwright Chromium 下载超时
**时间：** 2026年04月27日 00:15
**当前状态：**
1. 复盘部署失败日志，确认卡点发生在 Docker 构建阶段下载 Python Playwright wheel / Chromium 浏览器，GitHub Actions 远程部署总超时 30 分钟时被取消，根因是 `python -m playwright install --with-deps chromium` 下载体积过大且受网络速度影响明显。
2. 修改了 `Dockerfile`，改为通过 Debian 包直接安装系统级 `chromium`，同时删除构建阶段的 `python -m playwright install --with-deps chromium`，只保留 `pip3 install requests yt-dlp playwright`，将最慢的浏览器二进制下载步骤从部署链路中移除。
3. 修改了 `爬取每日密码.py`，为 `p.chromium.launch` 显式指定 `executable_path="/usr/bin/chromium"`，让 Python Playwright 直接复用容器内系统浏览器，避免继续依赖 Playwright 自带浏览器目录。
4. 下一步待办：重新触发部署，重点观察 `docker-compose build` 是否明显缩短，并在容器内验证每日密码抓取是否恢复正常。

## [当前节点] 切换国内镜像源继续压缩部署耗时
**时间：** 2026年04月27日 00:43
**当前状态：**
1. 根据最新部署日志确认，当前最大耗时已经转移到服务器访问 Debian 官方源安装系统 `chromium`，网络速率仅约 `55.4 kB/s`，因此进一步提速重点是镜像源而不是业务代码本身。
2. 修改了 `Dockerfile`，在系统依赖安装前先将 Debian `apt` 源切换为清华镜像，同时补充 `pip3` 清华源配置与 `npm` 的 `npmmirror` 配置，尽量让容器构建阶段所有下载都优先走国内镜像。
3. 将 `npm install` 改为 `npm ci`，利用现有 `package-lock.json` 做更稳定且通常更快的依赖安装，减少 Node 依赖解析时间。
4. 下一步待办：重新触发自动部署，观察 `apt-get update`、`apt-get install chromium`、`npm ci`、`pip3 install` 四段日志的实际提速幅度，再决定是否继续上“预构建基础镜像”方案。

更新时间：2026年04月27日 00:43

