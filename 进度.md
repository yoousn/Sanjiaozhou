项目进度存档

[当前节点] 梳理规则与修复环境依赖
时间：2026年04月24日 11:15 (北京时间)
当前状态：
1. 已经精简并更新了规则，明确了后续上传1Panel的最终目标。
2. 明确了公司和家里两台电脑开发的交接规则。
3. 发现本地环境执行Python采集时报错No module named yt_dlp。
4. 下一步待办：在本地Python环境安装相关依赖以使得搜索及采集功能可以正常跑通。

[当前节点] 强化工作流规范
时间：2026年04月24日 11:25 (北京时间)
当前状态：
1. 修改了规则，将“一切微小改动都必须记录在进度中”确立为绝对铁律。
2. 等待本地执行依赖安装（yt_dlp等），以验证采集脚本是否可以正常调用。

[当前节点] 规范时间记录格式
时间：2026年04月24日 11:32 (北京时间)
当前状态：
1. 修改了进度中的历史时间记录，将“刚刚”替换为具体的“年月日 时:分”格式。
2. 更新了规则，增加时间格式的书写规范，确保多端同步时时间线绝对清晰。

[当前节点] 修复 B 站抓取 SSL EOF 报错
时间：2026年04月24日 11:36 (北京时间)
当前状态：
1. 发现搜索请求视频详情时，B站报错[SSL: UNEXPECTED_EOF_WHILE_READING]，原因为B站风控主动断开连接。
2. 修改了后台脚本中的yt-dlp调用参数，增加了自动重试、忽略SSL校验以及浏览器UA伪装。
3. 重新测试搜索功能。

[当前节点] 优化模型测试与前端错误排查
时间：2026年04月24日 13:59 (北京时间)
当前状态：
1. 发现测试模型返回504且速度缓慢。
2. 增加了fetch-models模式，直接请求API以实现秒级模型列表加载。
3. 优化了test-model测试模式，将聊天测试耗时压缩到极短，并增加中文报错提示。
4. 前端异常记录：确认加入网站时出现变量未定义的报错。
5. 下一步待办：修复前端代码中的旧状态函数调用。

[当前节点] 修复前端确认加入时的变量未定义错误
时间：2026年04月24日 14:12 (北京时间)
当前状态：
1. 排查了“确认加入网站”时报错的问题。
2. 修正为最新状态管理机制，彻底修复确认加入后页面报错的问题。

[当前节点] 调整文档格式与统一使用北京时间
时间：2026年04月24日 14:14 (北京时间)
当前状态：
1. 按照要求去除了规则与进度文档中的所有 Markdown 格式标记，统一改为普通白色小字体的纯文本样式。
2. 统一将历史记录的时间调整为了对应的北京时间（UTC+8）。此后所有的进度更新都将使用北京时间。
3. 下一步待办：继续测试项目，确保手动和自动采集全链路无异常，准备打包部署 1Panel。

### 2026-04-24 更新日志：马坤时代 全面升级与重构

#### 🎨 界面与主题大焕新
1. **文案与品牌升级**：网站标题与描述全面升级为“坤坤改枪码”与“马坤时代 / Base”，并替换全局 Favicon 与侧边栏图标为 🐴。
2. **全屏暗黑模式**：基于 Tailwind CSS v4 实现了极具科幻感的全局暗黑模式，并在顶部导航栏加入“日/月”无缝切换按钮。
3. **超级外观设置中心**：在侧边栏底部新增“外观设置”，支持高度自定义主题强调色、日间/暗黑模式下的文字颜色及枪械名颜色，并提供一键恢复默认功能。
4. **统一卡片尺寸**：重构了 `GunCard` 容器高度逻辑，无论枪械包含几张配置卡片，所有大卡片在视觉上均保持绝对严格等高（固定 `h-[330px]`），消除了翻页时的跳动问题。

#### ✨ 核心交互功能新增
1. **智能搜索与补全**：全面升级搜索框，支持输入时实时弹出包含候选枪械的悬浮下拉菜单（智能补全）；搜索逻辑穿透分类限制，支持直接搜索具体的“改法名称”或“改枪代码”。
2. **卡片置顶功能**：在每张枪械卡片右上角新增“大头针（Pin）”按钮，支持一键置顶并热保存到服务器。置顶状态下配有绝美的淡黄色高亮及倾斜印章动画。编辑模式下依然支持置顶卡片组内部的拖拽排序。
3. **细化枪械分类**：将枪械库全面扩充为 7 大精准分类（突击步枪、战斗步枪、冲锋枪、轻机枪、精准射手步枪、狙击步枪、手枪），并同步更新了新增、编辑和 AI 识别的选项字典。
4. **隐藏干扰元素**：在进入“编辑模式”后，自动隐藏“采集”和“排序”等无关按钮，提供更沉浸的编辑体验。

#### 🛠️ 底层架构与 Docker 稳定性修复
1. **修复 Docker 数据持久化（黄金准则）**：彻底修复了单文件挂载导致保存失效的 Bug，将 `docker-compose.yml` 升级为挂载整个 `src` 和 `scripts` 目录，防止 Inode 变更导致映射断开。
2. **前端状态码拦截**：为“保存配置”等 API 提交增加了严谨的 `res.ok` 状态码校验，彻底拦截由反向代理缓存或 500 报错引起的“伪造成功”提示，失败时将精准弹出红字报错。
3. **Python AI 爬虫提示词优化**：强化了大模型 Prompt 的约束规则，强制 AI 将“链锯”、“高改”等修饰语拆分至 `buildType` 字段，彻底解决“链锯M249”等修饰语污染枪械基础名称的问题。
