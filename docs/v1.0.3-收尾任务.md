# v1.0.3 收尾任务 — 未完成项清单

> 上一轮已交付：社区功能完整闭环 + CollectModal 拆分 + App.tsx 表层提取。
> 本文档列出规划中尚未执行的部分，全部完成后删除。

---

## 一、App.tsx 深度拆分（当前 1856 行 → 目标 ~350 行）

### 1.1 提取 Hooks

| 文件 | 内容 | 预计行数 |
|------|------|----------|
| `src/hooks/useDailyPassword.ts` | syncDailyPwd、handleRefreshDailyPwd、stopDailyPwdPolling、scheduleDailyPwdPolling、applyDailyPwd、shouldRefreshDailyPwd、handleCopyDailyPwd 以及相关的 ref/timer/date-watcher useEffect | ~240 |
| `src/hooks/useTheme.ts` | isDarkMode / customTheme / uiPreferences 状态与 localStorage 持久化 useEffect | ~80 |

### 1.2 提取页面/面板组件

| 文件 | 内容 | 预计行数 |
|------|------|----------|
| `src/components/SettingsPage.tsx` | 设置页全部 JSX（数据管理、Cookie、每日密码日志、个性化设置） | ~140 |
| `src/components/DailyPwdCard.tsx` | 首页"今日密码"显示卡片 | ~50 |
| `src/components/EditCustomizePanel.tsx` | 编辑模式下的界面自定义面板（SliderField + 下拉） | ~80 |
| `src/components/ModeSelectModal.tsx` | 选择采集模式的子弹窗（手动/自动） | ~30 |
| `src/components/AutoCollectConfigModal.tsx` | 自动采集配置弹窗 | ~100 |

> SliderField 组件可在拆分后直接并入 `EditCustomizePanel.tsx` 或保留在 App.tsx。

---

## 二、server.ts 拆分（当前 1238 行 → 目标 ~120 行）

### 2.1 删除重复类型定义

- `server.ts` L41-213 有 ~170 行类型定义，与 `src/types.ts` 高度重复
- 改为从共享类型文件导入，或提取为 `server/types.ts` 一份，前后端各自引用

### 2.2 提取 lib（业务工具）

| 文件 | 来源 | 内容 |
|------|------|------|
| `server/lib/logs.ts` | L221-256 | 日志读写（getBeijingTimeString、readLogs、addLog、readAutoLogs 等） |
| `server/lib/shape.ts` | L299-365 | ensureVariantShape、ensureGroupShape、normalizeGunName、trimUniqueStrings |
| `server/lib/collectSettings.ts` | L366-505 | 采集配置读写、buildProviderMeta、buildCollectMeta、buildCollectSettingsDownload |
| `server/lib/merge.ts` | L507-582 | sortVariantsNewestFirst、mergeGroupVariants、mergeCollectedGroups |
| `server/lib/collector.ts` | L584-689 | runCollector、fetchModelsFromProvider、runSearchCollectorStream |

### 2.3 提取 routes（路由模块）

| 文件 | 来源 | 内容 |
|------|------|------|
| `server/routes/builds.ts` | `/api/builds` | GET/POST |
| `server/routes/collect.ts` | 采集相关 | `/api/collect/*` 全部路由 |
| `server/routes/config.ts` | 配置相关 | cookie、settings-file |
| `server/routes/dailyPassword.ts` | 每日密码 | `/api/daily-password/*` |

### 2.4 server.ts 收尾

拆分后 server.ts 只保留：
- Express app 创建
- 中间件挂载
- 路由注册
- Vite / 静态服务
- 每日密码 + 自动采集定时器启动

---

## 三、验证步骤

每个阶段完成后执行：
1. `npm run lint`（tsc --noEmit）
2. `npm run build`
3. 浏览器验证受影响功能
4. 更新本清单进度

---

## 四、进度记录

| 任务 | 状态 |
|------|------|
| useDailyPassword.ts 提取 | ⏳ 待开始 |
| useTheme.ts 提取 | ⏳ 待开始 |
| SettingsPage.tsx 提取 | ⏳ 待开始 |
| DailyPwdCard.tsx 提取 | ⏳ 待开始 |
| EditCustomizePanel.tsx 提取 | ⏳ 待开始 |
| ModeSelectModal.tsx 提取 | ⏳ 待开始 |
| AutoCollectConfigModal.tsx 提取 | ⏳ 待开始 |
| App.tsx 验证 | ⏳ 待开始 |
| server.ts 类型去重 | ⏳ 待开始 |
| server/lib/logs.ts | ⏳ 待开始 |
| server/lib/shape.ts | ⏳ 待开始 |
| server/lib/collectSettings.ts | ⏳ 待开始 |
| server/lib/merge.ts | ⏳ 待开始 |
| server/lib/collector.ts | ⏳ 待开始 |
| server/routes/builds.ts | ⏳ 待开始 |
| server/routes/collect.ts | ⏳ 待开始 |
| server/routes/config.ts | ⏳ 待开始 |
| server/routes/dailyPassword.ts | ⏳ 待开始 |
| server.ts 验证 | ⏳ 待开始 |
| 全部验证 | ⏳ 待开始 |
| 删除此文档 | ⏳ 待开始 |
